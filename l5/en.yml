success: |-
  All tests passed. Here's the output:

  ```
  %{output}
  ```
failure: |-
  When we ran the `rails db:setup && rails test` command, we saw one or
  more errors. What follows is the output of those commands.

  ```
  %{output}
  ```
